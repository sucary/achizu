# Explicitly unset DATABASE_URL to prevent connecting to Supabase
DATABASE_URL=

# Local test database (Docker)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=artist_map_test
DB_PASSWORD=testpassword
DB_PORT=5433